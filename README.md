# wing

A trade study on strut-braced and cantilever configurations of a 19-passenger utility transport aircraft wing spar structure.

## Environment quickstart

```bash
python -m pip install -U pip
python -m pip install -e ".[dev]"
python -m wing_trade_study.cli.main doctor
```

Detailed setup and diagnostics notes are in `docs/environment_setup.md`.
